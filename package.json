{
	"scripts": {
		"heroku-prebuild": "cd client && npm install && npm ../server && npm install",
		"build": "webpack --config server/webpack.config.ts",
		"start": "ts-node app.ts"
	},
	"engines": {
		"node": "10.15.1"
	}
}
